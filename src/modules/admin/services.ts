/**
 * --- CHATTRI ADMIN API CAPABILITIES ---
 * * [User Management]
 * - SEARCH: Query users by email, name, or UUID.
 * - FILTER: Segment by status (SUBSCRIBED, UNSUBSCRIBED), device (userAgent), or source.
 * - MODERATION: Manually update user status or blacklist spam emails.
 * * [Communications]
 * - CAMPAIGNS: Trigger bulk or single updates using customTemplate.
 * - PREVIEW: Send 'Draft' versions of emails to admin before broadcast.
 * - METRICS: Track delivery success/failure counts per broadcast.
 * * [Reporting & Analytics]
 * - GROWTH: Get daily/weekly signup velocity and conversion rates.
 * - LOGS: Full history of BroadcastLog to audit admin communications.
 * - EXPORT: Generate CSV of active 'SUBSCRIBED' users for external marketing.
 */